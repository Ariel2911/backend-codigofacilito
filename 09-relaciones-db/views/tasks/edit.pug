doctype html 
html 
  head 
    title Tareas
  body 
    h1= task.dataValues.id
    form(action= `/tasks/${task.dataValues.id}?_method=PUT` method="POST")
      textarea(name="description", cols="30", rows="10" placeholder="Coloca aquí el pendiente a realizar")= task.dataValues.description 
      input(type="submit" value="Guardar") 