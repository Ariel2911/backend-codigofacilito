doctype html 
html 
  head 
    title Tareas 
  body 
    h1= task.dataValues.description 
    p= task.dataValues.createdAt
    form(action= `/tasks/${task.dataValues.id}?_method=DELETE` method="POST")
      input(type="submit" value="Eliminar") 